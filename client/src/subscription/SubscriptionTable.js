import React from 'react'

const SubscriptionTable = ({ items, handleEdit, handleDelete, editing , handleUpdate, handleCancel }) => {
  return (
    <div>SubscriptionTable</div>
  )
}

export default SubscriptionTable